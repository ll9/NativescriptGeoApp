const BASE_URL = '';
const LOGIN_URL = '';
const REGISTER_URL = '';

export default class AuthService {
    login(email, password) {
        alert('login')
    }

    register(email, password) {
        alert('register');
    }
}